// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.7.5
//   protoc               v6.33.1
// source: sensor/camera_sensor.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";

export const protobufPackage = "proto.sensor";

export enum ImageFormat {
  UNKNOWN = 0,
  RGB = 1,
  BGR = 2,
  RGBA = 3,
  BGRA = 4,
  GRAY = 5,
  UNRECOGNIZED = -1,
}

export function imageFormatFromJSON(object: any): ImageFormat {
  switch (object) {
    case 0:
    case "UNKNOWN":
      return ImageFormat.UNKNOWN;
    case 1:
    case "RGB":
      return ImageFormat.RGB;
    case 2:
    case "BGR":
      return ImageFormat.BGR;
    case 3:
    case "RGBA":
      return ImageFormat.RGBA;
    case 4:
    case "BGRA":
      return ImageFormat.BGRA;
    case 5:
    case "GRAY":
      return ImageFormat.GRAY;
    case -1:
    case "UNRECOGNIZED":
    default:
      return ImageFormat.UNRECOGNIZED;
  }
}

export function imageFormatToJSON(object: ImageFormat): string {
  switch (object) {
    case ImageFormat.UNKNOWN:
      return "UNKNOWN";
    case ImageFormat.RGB:
      return "RGB";
    case ImageFormat.BGR:
      return "BGR";
    case ImageFormat.RGBA:
      return "RGBA";
    case ImageFormat.BGRA:
      return "BGRA";
    case ImageFormat.GRAY:
      return "GRAY";
    case ImageFormat.UNRECOGNIZED:
    default:
      return "UNRECOGNIZED";
  }
}

export enum ImageCompression {
  NONE = 0,
  JPEG = 1,
  PNG = 2,
  UNRECOGNIZED = -1,
}

export function imageCompressionFromJSON(object: any): ImageCompression {
  switch (object) {
    case 0:
    case "NONE":
      return ImageCompression.NONE;
    case 1:
    case "JPEG":
      return ImageCompression.JPEG;
    case 2:
    case "PNG":
      return ImageCompression.PNG;
    case -1:
    case "UNRECOGNIZED":
    default:
      return ImageCompression.UNRECOGNIZED;
  }
}

export function imageCompressionToJSON(object: ImageCompression): string {
  switch (object) {
    case ImageCompression.NONE:
      return "NONE";
    case ImageCompression.JPEG:
      return "JPEG";
    case ImageCompression.PNG:
      return "PNG";
    case ImageCompression.UNRECOGNIZED:
    default:
      return "UNRECOGNIZED";
  }
}

export interface ImageData {
  image: Uint8Array;
  height: number;
  width: number;
  format: ImageFormat;
  compression: ImageCompression;
}

function createBaseImageData(): ImageData {
  return { image: new Uint8Array(0), height: 0, width: 0, format: 0, compression: 0 };
}

export const ImageData: MessageFns<ImageData> = {
  encode(message: ImageData, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.image.length !== 0) {
      writer.uint32(10).bytes(message.image);
    }
    if (message.height !== 0) {
      writer.uint32(16).int32(message.height);
    }
    if (message.width !== 0) {
      writer.uint32(24).int32(message.width);
    }
    if (message.format !== 0) {
      writer.uint32(32).int32(message.format);
    }
    if (message.compression !== 0) {
      writer.uint32(40).int32(message.compression);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ImageData {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseImageData();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.image = reader.bytes();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.height = reader.int32();
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.width = reader.int32();
          continue;
        }
        case 4: {
          if (tag !== 32) {
            break;
          }

          message.format = reader.int32() as any;
          continue;
        }
        case 5: {
          if (tag !== 40) {
            break;
          }

          message.compression = reader.int32() as any;
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ImageData {
    return {
      image: isSet(object.image) ? bytesFromBase64(object.image) : new Uint8Array(0),
      height: isSet(object.height) ? globalThis.Number(object.height) : 0,
      width: isSet(object.width) ? globalThis.Number(object.width) : 0,
      format: isSet(object.format) ? imageFormatFromJSON(object.format) : 0,
      compression: isSet(object.compression) ? imageCompressionFromJSON(object.compression) : 0,
    };
  },

  toJSON(message: ImageData): unknown {
    const obj: any = {};
    if (message.image.length !== 0) {
      obj.image = base64FromBytes(message.image);
    }
    if (message.height !== 0) {
      obj.height = Math.round(message.height);
    }
    if (message.width !== 0) {
      obj.width = Math.round(message.width);
    }
    if (message.format !== 0) {
      obj.format = imageFormatToJSON(message.format);
    }
    if (message.compression !== 0) {
      obj.compression = imageCompressionToJSON(message.compression);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<ImageData>, I>>(base?: I): ImageData {
    return ImageData.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ImageData>, I>>(object: I): ImageData {
    const message = createBaseImageData();
    message.image = object.image ?? new Uint8Array(0);
    message.height = object.height ?? 0;
    message.width = object.width ?? 0;
    message.format = object.format ?? 0;
    message.compression = object.compression ?? 0;
    return message;
  },
};

function bytesFromBase64(b64: string): Uint8Array {
  if ((globalThis as any).Buffer) {
    return Uint8Array.from(globalThis.Buffer.from(b64, "base64"));
  } else {
    const bin = globalThis.atob(b64);
    const arr = new Uint8Array(bin.length);
    for (let i = 0; i < bin.length; ++i) {
      arr[i] = bin.charCodeAt(i);
    }
    return arr;
  }
}

function base64FromBytes(arr: Uint8Array): string {
  if ((globalThis as any).Buffer) {
    return globalThis.Buffer.from(arr).toString("base64");
  } else {
    const bin: string[] = [];
    arr.forEach((byte) => {
      bin.push(globalThis.String.fromCharCode(byte));
    });
    return globalThis.btoa(bin.join(""));
  }
}

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
export type Exact<P, I extends P> = P extends Builtin ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P>>]: never };

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create<I extends Exact<DeepPartial<T>, I>>(base?: I): T;
  fromPartial<I extends Exact<DeepPartial<T>, I>>(object: I): T;
}
