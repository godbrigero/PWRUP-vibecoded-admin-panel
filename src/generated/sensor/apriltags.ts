// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.7.5
//   protoc               v6.32.1
// source: sensor/apriltags.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";
import { Rotation3d } from "../util/position";
import { Vector2, Vector3 } from "../util/vector";

export const protobufPackage = "proto.sensor";

export interface AprilTagData {
  worldTags?: WorldTags | undefined;
  rawTags?: RawTags | undefined;
}

export interface WorldTags {
  tags: ProcessedTag[];
}

export interface RawTags {
  corners: UnprocessedTag[];
}

export interface ProcessedTag {
  id: number;
  poseR: number[];
  poseT: number[];
  positionWPILib?: Vector3 | undefined;
  rotationWPILib?: Rotation3d | undefined;
}

export interface UnprocessedTag {
  id: number;
  corners: Vector2[];
}

function createBaseAprilTagData(): AprilTagData {
  return { worldTags: undefined, rawTags: undefined };
}

export const AprilTagData: MessageFns<AprilTagData> = {
  encode(message: AprilTagData, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.worldTags !== undefined) {
      WorldTags.encode(message.worldTags, writer.uint32(10).fork()).join();
    }
    if (message.rawTags !== undefined) {
      RawTags.encode(message.rawTags, writer.uint32(18).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): AprilTagData {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseAprilTagData();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.worldTags = WorldTags.decode(reader, reader.uint32());
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.rawTags = RawTags.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): AprilTagData {
    return {
      worldTags: isSet(object.worldTags) ? WorldTags.fromJSON(object.worldTags) : undefined,
      rawTags: isSet(object.rawTags) ? RawTags.fromJSON(object.rawTags) : undefined,
    };
  },

  toJSON(message: AprilTagData): unknown {
    const obj: any = {};
    if (message.worldTags !== undefined) {
      obj.worldTags = WorldTags.toJSON(message.worldTags);
    }
    if (message.rawTags !== undefined) {
      obj.rawTags = RawTags.toJSON(message.rawTags);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<AprilTagData>, I>>(base?: I): AprilTagData {
    return AprilTagData.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<AprilTagData>, I>>(object: I): AprilTagData {
    const message = createBaseAprilTagData();
    message.worldTags = (object.worldTags !== undefined && object.worldTags !== null)
      ? WorldTags.fromPartial(object.worldTags)
      : undefined;
    message.rawTags = (object.rawTags !== undefined && object.rawTags !== null)
      ? RawTags.fromPartial(object.rawTags)
      : undefined;
    return message;
  },
};

function createBaseWorldTags(): WorldTags {
  return { tags: [] };
}

export const WorldTags: MessageFns<WorldTags> = {
  encode(message: WorldTags, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    for (const v of message.tags) {
      ProcessedTag.encode(v!, writer.uint32(10).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): WorldTags {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseWorldTags();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.tags.push(ProcessedTag.decode(reader, reader.uint32()));
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): WorldTags {
    return {
      tags: globalThis.Array.isArray(object?.tags) ? object.tags.map((e: any) => ProcessedTag.fromJSON(e)) : [],
    };
  },

  toJSON(message: WorldTags): unknown {
    const obj: any = {};
    if (message.tags?.length) {
      obj.tags = message.tags.map((e) => ProcessedTag.toJSON(e));
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<WorldTags>, I>>(base?: I): WorldTags {
    return WorldTags.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<WorldTags>, I>>(object: I): WorldTags {
    const message = createBaseWorldTags();
    message.tags = object.tags?.map((e) => ProcessedTag.fromPartial(e)) || [];
    return message;
  },
};

function createBaseRawTags(): RawTags {
  return { corners: [] };
}

export const RawTags: MessageFns<RawTags> = {
  encode(message: RawTags, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    for (const v of message.corners) {
      UnprocessedTag.encode(v!, writer.uint32(10).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): RawTags {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseRawTags();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.corners.push(UnprocessedTag.decode(reader, reader.uint32()));
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): RawTags {
    return {
      corners: globalThis.Array.isArray(object?.corners)
        ? object.corners.map((e: any) => UnprocessedTag.fromJSON(e))
        : [],
    };
  },

  toJSON(message: RawTags): unknown {
    const obj: any = {};
    if (message.corners?.length) {
      obj.corners = message.corners.map((e) => UnprocessedTag.toJSON(e));
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<RawTags>, I>>(base?: I): RawTags {
    return RawTags.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<RawTags>, I>>(object: I): RawTags {
    const message = createBaseRawTags();
    message.corners = object.corners?.map((e) => UnprocessedTag.fromPartial(e)) || [];
    return message;
  },
};

function createBaseProcessedTag(): ProcessedTag {
  return { id: 0, poseR: [], poseT: [], positionWPILib: undefined, rotationWPILib: undefined };
}

export const ProcessedTag: MessageFns<ProcessedTag> = {
  encode(message: ProcessedTag, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.id !== 0) {
      writer.uint32(8).int32(message.id);
    }
    writer.uint32(18).fork();
    for (const v of message.poseR) {
      writer.float(v);
    }
    writer.join();
    writer.uint32(26).fork();
    for (const v of message.poseT) {
      writer.float(v);
    }
    writer.join();
    if (message.positionWPILib !== undefined) {
      Vector3.encode(message.positionWPILib, writer.uint32(34).fork()).join();
    }
    if (message.rotationWPILib !== undefined) {
      Rotation3d.encode(message.rotationWPILib, writer.uint32(42).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ProcessedTag {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseProcessedTag();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.id = reader.int32();
          continue;
        }
        case 2: {
          if (tag === 21) {
            message.poseR.push(reader.float());

            continue;
          }

          if (tag === 18) {
            const end2 = reader.uint32() + reader.pos;
            while (reader.pos < end2) {
              message.poseR.push(reader.float());
            }

            continue;
          }

          break;
        }
        case 3: {
          if (tag === 29) {
            message.poseT.push(reader.float());

            continue;
          }

          if (tag === 26) {
            const end2 = reader.uint32() + reader.pos;
            while (reader.pos < end2) {
              message.poseT.push(reader.float());
            }

            continue;
          }

          break;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.positionWPILib = Vector3.decode(reader, reader.uint32());
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          message.rotationWPILib = Rotation3d.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ProcessedTag {
    return {
      id: isSet(object.id) ? globalThis.Number(object.id) : 0,
      poseR: globalThis.Array.isArray(object?.poseR) ? object.poseR.map((e: any) => globalThis.Number(e)) : [],
      poseT: globalThis.Array.isArray(object?.poseT) ? object.poseT.map((e: any) => globalThis.Number(e)) : [],
      positionWPILib: isSet(object.positionWPILib) ? Vector3.fromJSON(object.positionWPILib) : undefined,
      rotationWPILib: isSet(object.rotationWPILib) ? Rotation3d.fromJSON(object.rotationWPILib) : undefined,
    };
  },

  toJSON(message: ProcessedTag): unknown {
    const obj: any = {};
    if (message.id !== 0) {
      obj.id = Math.round(message.id);
    }
    if (message.poseR?.length) {
      obj.poseR = message.poseR;
    }
    if (message.poseT?.length) {
      obj.poseT = message.poseT;
    }
    if (message.positionWPILib !== undefined) {
      obj.positionWPILib = Vector3.toJSON(message.positionWPILib);
    }
    if (message.rotationWPILib !== undefined) {
      obj.rotationWPILib = Rotation3d.toJSON(message.rotationWPILib);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<ProcessedTag>, I>>(base?: I): ProcessedTag {
    return ProcessedTag.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ProcessedTag>, I>>(object: I): ProcessedTag {
    const message = createBaseProcessedTag();
    message.id = object.id ?? 0;
    message.poseR = object.poseR?.map((e) => e) || [];
    message.poseT = object.poseT?.map((e) => e) || [];
    message.positionWPILib = (object.positionWPILib !== undefined && object.positionWPILib !== null)
      ? Vector3.fromPartial(object.positionWPILib)
      : undefined;
    message.rotationWPILib = (object.rotationWPILib !== undefined && object.rotationWPILib !== null)
      ? Rotation3d.fromPartial(object.rotationWPILib)
      : undefined;
    return message;
  },
};

function createBaseUnprocessedTag(): UnprocessedTag {
  return { id: 0, corners: [] };
}

export const UnprocessedTag: MessageFns<UnprocessedTag> = {
  encode(message: UnprocessedTag, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.id !== 0) {
      writer.uint32(8).int32(message.id);
    }
    for (const v of message.corners) {
      Vector2.encode(v!, writer.uint32(18).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): UnprocessedTag {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseUnprocessedTag();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.id = reader.int32();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.corners.push(Vector2.decode(reader, reader.uint32()));
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): UnprocessedTag {
    return {
      id: isSet(object.id) ? globalThis.Number(object.id) : 0,
      corners: globalThis.Array.isArray(object?.corners) ? object.corners.map((e: any) => Vector2.fromJSON(e)) : [],
    };
  },

  toJSON(message: UnprocessedTag): unknown {
    const obj: any = {};
    if (message.id !== 0) {
      obj.id = Math.round(message.id);
    }
    if (message.corners?.length) {
      obj.corners = message.corners.map((e) => Vector2.toJSON(e));
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<UnprocessedTag>, I>>(base?: I): UnprocessedTag {
    return UnprocessedTag.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<UnprocessedTag>, I>>(object: I): UnprocessedTag {
    const message = createBaseUnprocessedTag();
    message.id = object.id ?? 0;
    message.corners = object.corners?.map((e) => Vector2.fromPartial(e)) || [];
    return message;
  },
};

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
export type Exact<P, I extends P> = P extends Builtin ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P>>]: never };

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create<I extends Exact<DeepPartial<T>, I>>(base?: I): T;
  fromPartial<I extends Exact<DeepPartial<T>, I>>(object: I): T;
}
