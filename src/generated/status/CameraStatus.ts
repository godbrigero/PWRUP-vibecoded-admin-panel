// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.7.5
//   protoc               v6.32.1
// source: status/CameraStatus.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";

export const protobufPackage = "proto.status";

export interface CameraStatus {
  name: string;
  frameTime: number;
  inferenceTime: number;
}

function createBaseCameraStatus(): CameraStatus {
  return { name: "", frameTime: 0, inferenceTime: 0 };
}

export const CameraStatus: MessageFns<CameraStatus> = {
  encode(message: CameraStatus, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.name !== "") {
      writer.uint32(10).string(message.name);
    }
    if (message.frameTime !== 0) {
      writer.uint32(21).float(message.frameTime);
    }
    if (message.inferenceTime !== 0) {
      writer.uint32(29).float(message.inferenceTime);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): CameraStatus {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseCameraStatus();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.name = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 21) {
            break;
          }

          message.frameTime = reader.float();
          continue;
        }
        case 3: {
          if (tag !== 29) {
            break;
          }

          message.inferenceTime = reader.float();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): CameraStatus {
    return {
      name: isSet(object.name) ? globalThis.String(object.name) : "",
      frameTime: isSet(object.frameTime) ? globalThis.Number(object.frameTime) : 0,
      inferenceTime: isSet(object.inferenceTime) ? globalThis.Number(object.inferenceTime) : 0,
    };
  },

  toJSON(message: CameraStatus): unknown {
    const obj: any = {};
    if (message.name !== "") {
      obj.name = message.name;
    }
    if (message.frameTime !== 0) {
      obj.frameTime = message.frameTime;
    }
    if (message.inferenceTime !== 0) {
      obj.inferenceTime = message.inferenceTime;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<CameraStatus>, I>>(base?: I): CameraStatus {
    return CameraStatus.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<CameraStatus>, I>>(object: I): CameraStatus {
    const message = createBaseCameraStatus();
    message.name = object.name ?? "";
    message.frameTime = object.frameTime ?? 0;
    message.inferenceTime = object.inferenceTime ?? 0;
    return message;
  },
};

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
export type Exact<P, I extends P> = P extends Builtin ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P>>]: never };

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create<I extends Exact<DeepPartial<T>, I>>(base?: I): T;
  fromPartial<I extends Exact<DeepPartial<T>, I>>(object: I): T;
}
