// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.7.5
//   protoc               v5.29.3
// source: status/PiStatus.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";

export const protobufPackage = "proto.status";

export enum StatusType {
  SYSTEM_STATUS = 0,
  LOG_MESSAGE = 1,
  UNRECOGNIZED = -1,
}

export function statusTypeFromJSON(object: any): StatusType {
  switch (object) {
    case 0:
    case "SYSTEM_STATUS":
      return StatusType.SYSTEM_STATUS;
    case 1:
    case "LOG_MESSAGE":
      return StatusType.LOG_MESSAGE;
    case -1:
    case "UNRECOGNIZED":
    default:
      return StatusType.UNRECOGNIZED;
  }
}

export function statusTypeToJSON(object: StatusType): string {
  switch (object) {
    case StatusType.SYSTEM_STATUS:
      return "SYSTEM_STATUS";
    case StatusType.LOG_MESSAGE:
      return "LOG_MESSAGE";
    case StatusType.UNRECOGNIZED:
    default:
      return "UNRECOGNIZED";
  }
}

export interface StatusBase {
  type: StatusType;
}

export interface PiProcess {
  name: string;
  pid: number;
  cpuUsage: number;
}

export interface PiStatus {
  type: StatusType;
  piName: string;
  cpuUsageCores: number[];
  cpuUsageTotal: number;
  memoryUsage: number;
  diskUsage: number;
  netUsageIn: number;
  netUsageOut: number;
  top10Processes: PiProcess[];
}

export interface LogMessage {
  type: StatusType;
  prefix: string;
  message: string;
  color: string;
}

function createBaseStatusBase(): StatusBase {
  return { type: 0 };
}

export const StatusBase: MessageFns<StatusBase> = {
  encode(message: StatusBase, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.type !== 0) {
      writer.uint32(8).int32(message.type);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): StatusBase {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseStatusBase();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.type = reader.int32() as any;
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): StatusBase {
    return { type: isSet(object.type) ? statusTypeFromJSON(object.type) : 0 };
  },

  toJSON(message: StatusBase): unknown {
    const obj: any = {};
    if (message.type !== 0) {
      obj.type = statusTypeToJSON(message.type);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<StatusBase>, I>>(base?: I): StatusBase {
    return StatusBase.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<StatusBase>, I>>(object: I): StatusBase {
    const message = createBaseStatusBase();
    message.type = object.type ?? 0;
    return message;
  },
};

function createBasePiProcess(): PiProcess {
  return { name: "", pid: 0, cpuUsage: 0 };
}

export const PiProcess: MessageFns<PiProcess> = {
  encode(message: PiProcess, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.name !== "") {
      writer.uint32(10).string(message.name);
    }
    if (message.pid !== 0) {
      writer.uint32(16).int32(message.pid);
    }
    if (message.cpuUsage !== 0) {
      writer.uint32(29).float(message.cpuUsage);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): PiProcess {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBasePiProcess();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.name = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.pid = reader.int32();
          continue;
        }
        case 3: {
          if (tag !== 29) {
            break;
          }

          message.cpuUsage = reader.float();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): PiProcess {
    return {
      name: isSet(object.name) ? globalThis.String(object.name) : "",
      pid: isSet(object.pid) ? globalThis.Number(object.pid) : 0,
      cpuUsage: isSet(object.cpuUsage) ? globalThis.Number(object.cpuUsage) : 0,
    };
  },

  toJSON(message: PiProcess): unknown {
    const obj: any = {};
    if (message.name !== "") {
      obj.name = message.name;
    }
    if (message.pid !== 0) {
      obj.pid = Math.round(message.pid);
    }
    if (message.cpuUsage !== 0) {
      obj.cpuUsage = message.cpuUsage;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<PiProcess>, I>>(base?: I): PiProcess {
    return PiProcess.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<PiProcess>, I>>(object: I): PiProcess {
    const message = createBasePiProcess();
    message.name = object.name ?? "";
    message.pid = object.pid ?? 0;
    message.cpuUsage = object.cpuUsage ?? 0;
    return message;
  },
};

function createBasePiStatus(): PiStatus {
  return {
    type: 0,
    piName: "",
    cpuUsageCores: [],
    cpuUsageTotal: 0,
    memoryUsage: 0,
    diskUsage: 0,
    netUsageIn: 0,
    netUsageOut: 0,
    top10Processes: [],
  };
}

export const PiStatus: MessageFns<PiStatus> = {
  encode(message: PiStatus, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.type !== 0) {
      writer.uint32(8).int32(message.type);
    }
    if (message.piName !== "") {
      writer.uint32(18).string(message.piName);
    }
    writer.uint32(26).fork();
    for (const v of message.cpuUsageCores) {
      writer.float(v);
    }
    writer.join();
    if (message.cpuUsageTotal !== 0) {
      writer.uint32(37).float(message.cpuUsageTotal);
    }
    if (message.memoryUsage !== 0) {
      writer.uint32(45).float(message.memoryUsage);
    }
    if (message.diskUsage !== 0) {
      writer.uint32(53).float(message.diskUsage);
    }
    if (message.netUsageIn !== 0) {
      writer.uint32(61).float(message.netUsageIn);
    }
    if (message.netUsageOut !== 0) {
      writer.uint32(69).float(message.netUsageOut);
    }
    for (const v of message.top10Processes) {
      PiProcess.encode(v!, writer.uint32(74).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): PiStatus {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBasePiStatus();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.type = reader.int32() as any;
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.piName = reader.string();
          continue;
        }
        case 3: {
          if (tag === 29) {
            message.cpuUsageCores.push(reader.float());

            continue;
          }

          if (tag === 26) {
            const end2 = reader.uint32() + reader.pos;
            while (reader.pos < end2) {
              message.cpuUsageCores.push(reader.float());
            }

            continue;
          }

          break;
        }
        case 4: {
          if (tag !== 37) {
            break;
          }

          message.cpuUsageTotal = reader.float();
          continue;
        }
        case 5: {
          if (tag !== 45) {
            break;
          }

          message.memoryUsage = reader.float();
          continue;
        }
        case 6: {
          if (tag !== 53) {
            break;
          }

          message.diskUsage = reader.float();
          continue;
        }
        case 7: {
          if (tag !== 61) {
            break;
          }

          message.netUsageIn = reader.float();
          continue;
        }
        case 8: {
          if (tag !== 69) {
            break;
          }

          message.netUsageOut = reader.float();
          continue;
        }
        case 9: {
          if (tag !== 74) {
            break;
          }

          message.top10Processes.push(PiProcess.decode(reader, reader.uint32()));
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): PiStatus {
    return {
      type: isSet(object.type) ? statusTypeFromJSON(object.type) : 0,
      piName: isSet(object.piName) ? globalThis.String(object.piName) : "",
      cpuUsageCores: globalThis.Array.isArray(object?.cpuUsageCores)
        ? object.cpuUsageCores.map((e: any) => globalThis.Number(e))
        : [],
      cpuUsageTotal: isSet(object.cpuUsageTotal) ? globalThis.Number(object.cpuUsageTotal) : 0,
      memoryUsage: isSet(object.memoryUsage) ? globalThis.Number(object.memoryUsage) : 0,
      diskUsage: isSet(object.diskUsage) ? globalThis.Number(object.diskUsage) : 0,
      netUsageIn: isSet(object.netUsageIn) ? globalThis.Number(object.netUsageIn) : 0,
      netUsageOut: isSet(object.netUsageOut) ? globalThis.Number(object.netUsageOut) : 0,
      top10Processes: globalThis.Array.isArray(object?.top10Processes)
        ? object.top10Processes.map((e: any) => PiProcess.fromJSON(e))
        : [],
    };
  },

  toJSON(message: PiStatus): unknown {
    const obj: any = {};
    if (message.type !== 0) {
      obj.type = statusTypeToJSON(message.type);
    }
    if (message.piName !== "") {
      obj.piName = message.piName;
    }
    if (message.cpuUsageCores?.length) {
      obj.cpuUsageCores = message.cpuUsageCores;
    }
    if (message.cpuUsageTotal !== 0) {
      obj.cpuUsageTotal = message.cpuUsageTotal;
    }
    if (message.memoryUsage !== 0) {
      obj.memoryUsage = message.memoryUsage;
    }
    if (message.diskUsage !== 0) {
      obj.diskUsage = message.diskUsage;
    }
    if (message.netUsageIn !== 0) {
      obj.netUsageIn = message.netUsageIn;
    }
    if (message.netUsageOut !== 0) {
      obj.netUsageOut = message.netUsageOut;
    }
    if (message.top10Processes?.length) {
      obj.top10Processes = message.top10Processes.map((e) => PiProcess.toJSON(e));
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<PiStatus>, I>>(base?: I): PiStatus {
    return PiStatus.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<PiStatus>, I>>(object: I): PiStatus {
    const message = createBasePiStatus();
    message.type = object.type ?? 0;
    message.piName = object.piName ?? "";
    message.cpuUsageCores = object.cpuUsageCores?.map((e) => e) || [];
    message.cpuUsageTotal = object.cpuUsageTotal ?? 0;
    message.memoryUsage = object.memoryUsage ?? 0;
    message.diskUsage = object.diskUsage ?? 0;
    message.netUsageIn = object.netUsageIn ?? 0;
    message.netUsageOut = object.netUsageOut ?? 0;
    message.top10Processes = object.top10Processes?.map((e) => PiProcess.fromPartial(e)) || [];
    return message;
  },
};

function createBaseLogMessage(): LogMessage {
  return { type: 0, prefix: "", message: "", color: "" };
}

export const LogMessage: MessageFns<LogMessage> = {
  encode(message: LogMessage, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.type !== 0) {
      writer.uint32(8).int32(message.type);
    }
    if (message.prefix !== "") {
      writer.uint32(18).string(message.prefix);
    }
    if (message.message !== "") {
      writer.uint32(26).string(message.message);
    }
    if (message.color !== "") {
      writer.uint32(34).string(message.color);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): LogMessage {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseLogMessage();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.type = reader.int32() as any;
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.prefix = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.message = reader.string();
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.color = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): LogMessage {
    return {
      type: isSet(object.type) ? statusTypeFromJSON(object.type) : 0,
      prefix: isSet(object.prefix) ? globalThis.String(object.prefix) : "",
      message: isSet(object.message) ? globalThis.String(object.message) : "",
      color: isSet(object.color) ? globalThis.String(object.color) : "",
    };
  },

  toJSON(message: LogMessage): unknown {
    const obj: any = {};
    if (message.type !== 0) {
      obj.type = statusTypeToJSON(message.type);
    }
    if (message.prefix !== "") {
      obj.prefix = message.prefix;
    }
    if (message.message !== "") {
      obj.message = message.message;
    }
    if (message.color !== "") {
      obj.color = message.color;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<LogMessage>, I>>(base?: I): LogMessage {
    return LogMessage.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<LogMessage>, I>>(object: I): LogMessage {
    const message = createBaseLogMessage();
    message.type = object.type ?? 0;
    message.prefix = object.prefix ?? "";
    message.message = object.message ?? "";
    message.color = object.color ?? "";
    return message;
  },
};

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
export type Exact<P, I extends P> = P extends Builtin ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P>>]: never };

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create<I extends Exact<DeepPartial<T>, I>>(base?: I): T;
  fromPartial<I extends Exact<DeepPartial<T>, I>>(object: I): T;
}
