syntax = "proto3";

import "util/vector.proto";
import "util/position.proto";

package proto.sensor;

message ImuData {
    util.Position3d position = 1;

    util.Vector3 velocity = 2;
    util.Vector3 acceleration = 3;

    util.Vector3 angularVelocityXYZ = 4;
}